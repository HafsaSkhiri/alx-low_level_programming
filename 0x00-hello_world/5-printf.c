#include <stdio.h>
/**
  *main - A program prints out a string to stdout
  *Return: 0 (Succes)
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
